# Rail Balance Checkpoints

Checkpoint states: `PLANNED`, `IN_PROGRESS`, `PASS`, `FAIL`, `BLOCKED`,
`DEFERRED_ENVIRONMENT`, or `SUPERSEDED`.

| ID | Scope | State | Evidence / exit condition |
| --- | --- | --- | --- |
| C000 | Read all supplied context and audit the current implementation | PASS | Both shared chats decoded/read; prompt design reviewed; code anchors and base commit recorded in `CONTEXT.md`. |
| C001 | Create isolated branch and persistent records | PASS | Verified branch `feat/rail-balance-prototype`; plan, context, development, optimization, and checkpoint records are present under `docs/rail_balance/`. |
| C010 | Existing-build and minimal EP8 correctness baseline | PASS | In-place SM90 build/import passed; EP8 direct-path first FP8/alignment-128 case passed at hidden 256 with performance disabled. The invalid hidden-128 attempt is retained in `DEVELOPMENT_LOG.md`. |
| C020 | CPU planner contract and deterministic oracle | PASS | Independent review passed; the 28-test direct runner covers 8,184 exhaustive quota cases, 64 seeded route/slot cases, strict logical-plan mutation validation, channel striping, and atomic capacity bypass. |
| C030 | GPU planner equals CPU oracle | PASS | Fresh-cache private JIT matches the canonical CPU oracle in 876 device-cases across isolated devices 0 and 7; invalid ABI, non-default stream, int64 offsets, and stable same-process cross-device rejection pass independent final review. |
| C040 | 8x1 copy-exact, one-shot direct-final source shuffle | PASS | Fresh-cache 8-GPU H7168 LSA/TMA run moves 256 records from all eight owners and exactly fills all eight 32-record egress arenas; full-record bytes, headroom poison, generation, physical-capacity caller bypass, source immutability, global coverage, independent review, and original EP8 direct regression pass. |
| C050 | One-shot proxy publication ordering and liveness | PASS | CPU oracle passes 34/34 and an independent 167,481-state exhaustion. Fresh-cache 8-GPU SM90 v6 passes 16 generations plus forced holes, stale values, byte-exact live payload consumption, a slow consumer, progress-aware late gating whose aggregate wait exceeds its watchdog, bounded missing-slot failure, and system-scope error arbitration. C040 and original EP8 regressions pass; two independent reviews report no Blocker/High/Medium. Ring reuse remains deferred. |
| C060 | Restricted 4x2 full dispatch/combine round trip | PASS | CPU full-egress oracle passes 36/36; six H7168/K4 SM90 kernels compile with 0 spill; fresh-cache H256 and H7168 8-GPU loops pass exact payload/route/combine checks, an independent layout formula, and an intact 4 KiB tail guard. A post-review fresh-cache run also passes token-rotated lane-to-expert routing. C040, C050, original EP8, core rebuild, and two independent final reviews pass with no unresolved Blocker/High/Medium. |
| C061 | 2x4 per-destination multi-node emulation | PASS | Fresh-cache H256 and H7168 eight-GPU runs pass 33 destination copies, six required per-destination moves, three quota holes, 72 expert contributions, and 282 byte-exact global coverage records. Planner 38/38, C060, original EP8, full build, and independent review pass with 0 Blocker/High/Medium. |
| C070 | Minimum cross-call route replay | PASS | H256/H7168 eight-GPU capture plus two manifest-free replays pass exact independent partial/output, immutable owning snapshots, zero status, and intact guard. One corrupt live route reports RouteMismatch=4 and exits collectively. Review closes 0 Blocker/High/Low; one production-only Medium requires cross-rank preflight consensus before C080 exposes the path. |
| C080 | Optional Hybrid integration with default off | DEFERRED_ENVIRONMENT | `LOCAL_COMPLETE / MULTINODE_PENDING`. The accepted force-v1 uses grouped static slots, a 4-byte transit key, existing Tag0, and one post-unshuffle barrier; rejected sidecar/descriptor/ring designs never entered production. GPU plan/source/return cores, isolated force codegen, owning transactions, constructor consensus, public one-shot lifecycle, default-off regression, fault/recovery, and focused sanitizer closure pass with no unresolved Blocker/High/Medium. Both capability bits remain false. Only truthful D>1 Rail/Gin runtime can advance this checkpoint. |
| C080-A | Contract, baseline fingerprints, and off identity | PASS | Keyword-only off/force config and a disabled dual capability gate are committed. Off preserves native sizing/runtime arguments, GPU buffer, JIT, handle/result identity, with only local mode/ticket guards; API 9/9, legacy goldens 4/4, default-off EP8 smoke, and independent review pass. |
| C080-B | GPU count, plan, and grouped proxy schedule | PASS | B1 matches 70 exact H200 materializer cases, including D32>K4. B2 then publishes per-rank compact counts and passes a fresh-cache true 8-GPU LSA snapshot: all fourteen outputs equal the CPU oracle for variable/zero N, phase reuse, Pcap 5/4, route/config recovery, C1024/D32, int64 seed, and non-default stream. No manifest/descriptor/ring exists. |
| C080-C | Minimal force arena and collective preflight | PASS | The checked 32B/2MiB force arena, fixed WORLD gates, owning H3-H5 private transaction, and H6 constructor/public one-shot lifecycle are complete while off retains native sizing/runtime arguments, GPU buffer, JIT, and result identity; only local mode/ticket guards are added. The final real EP8 watchdog performs mixed-mode and rank-local-invalid pre-comm rejects, creates and destroys a unanimous force window, then drives the public D1 dispatch rejection twice. It observes exactly six fixed MAX gates, stable constructor storage, invocation 1→3, no live ticket, retryable nonterminal state, and restored false capability. Constructor 9/9, fake public faults, API 9/9, legacy 4/4, focused H3-H5 contracts, and watchdog audits pass with Blocker0/High0. This is truthful D1 control-plane/fail-close evidence, not D>1 Gin execution. |
| C080-D | Shared-core LSA data path without a supplied manifest | PASS | Direct-final source shuffle plus static return-unshuffle pass true 8-GPU H256/H7168 and D32>K4 raw TokenLayout comparison, exact owner row/token routing, poison-preserved non-targets, exact Pcap, corruption/recovery, and non-default streams. The audited two-window bridge passes complete 4x2 and 2x4 H256/H7168 loops, rounding, zero-token deficit egress, reused non-default-stream arena, and all-zero work. Source/world plans, compact quota, proxy active/tail bytes, vnode records/routes/readies, stage status, guard, reduce rows, production return-unshuffle, and unchanged legacy epilogue are exact. A final true-EP8 case corrupts world plan state on rank 0, removes a plan tensor on rank 1, proves both are detected before B0 and abort collectively, then completes the next generation on the same buffers. Fault review is Blocker0/High0. Focused source mem/sync/device-init checks report zero errors; racecheck retains six explained WAW warnings. Non-default execution remains compatibility rather than a synchronization-free event-ordering claim. |
| C080-E | Isolated Hybrid dispatch specialization | PASS | Local evidence label: `HYBRID_CODEGEN_PASS`. A separate non-cached BF16 header/runtime compiles production-shaped G8xD2 H7168/K8, G4xD2 H7168/K4, and G2xD4 H256/H7168 (including D>K) force plus legacy cubins with recursive include hashes. Owner sends only retained prefixes; egress consumes `group_prefix+u`, sends to `retained+u`, then publishes one dense final tail. Forward metadata is exactly `3+2K`; `proxy_required` is ABI-only after trusted Gate2; plan tensors derive exact dispatch-payload puts/bytes without hot-path atomics. All eight cubins have STACK96/SPILL0; force REG is 64/64/69/64 versus legacy 68/68/67/67. Constraints 8/8, API 6/6, recursive legacy goldens 4/4, full build, static ABI/order checks, and post-Tag0 legacy-parity checks pass. This is codegen, not real Gin runtime evidence. |
| C080-F | Isolated Hybrid combine plus return-unshuffle | PASS | The production-shared return-unshuffle passes true 8-GPU H256/H7168 and D32>K4 byte-exact tests and compiles at REG60/STACK72/LOCAL0 with no spill. The isolated force combine changes only moved-record return destination, preserves `3+2K` replay and legacy reduction/completion, and covers all four `(D<=K,G<=K)` layouts. Six force/legacy pairs are each REG216/STACK96/SHARED1024/LOCAL0 with zero spill; only the force pointer ABI adds 8B constant memory. Constraints 8/8, API 6/6, legacy goldens 4/4, exact normalized source identity, build, and independent review pass. Evidence is `HYBRID_CODEGEN_PASS`; real Gin remains C080-H. |
| C080-G | Local closure and independent review | PASS | Final-tree default-off EP8 in both direct and truthful D1 Hybrid modes, planner/API/layout/legacy and H3-H6 contracts, 8x1 source/return, 4x2 and D>K 2x4 vnode, source/world-plan faults, fresh force/legacy codegen, and full build pass. A focused D>K vnode run reports zero memcheck, synccheck, initcheck, and racecheck errors; racecheck has zero hazards/warnings. Final review is Blocker0/High0/Medium0. Local state is `LOCAL_COMPLETE / MULTINODE_PENDING`. |
| C080-H | Real multi-node Rail/Gin runtime gate | DEFERRED_ENVIRONMENT | Requires truthful scaleout>1 Rail teams; only this gate can mark C080 `MULTINODE_PASS`. |
| C090 | Exhaustive local correctness, sanitizer, and fault campaign | IN_PROGRESS | Planner boundaries/random distributions, 8x1/4x2/2x4, capacity/reuse, source/world-plan faults, and the final D>K vnode memcheck/synccheck/initcheck/racecheck closure pass. Remaining truthful gaps are explicit named Zipf/log-normal routing fixtures, GPU stale/corrupt transit-key `p` injection on the current descriptor-free path, and a rank-selective delayed-rank/liveness case on that same path. Historical queue slow-consumer evidence cannot substitute for the final force-v1 protocol. |
| C100 | Controlled local performance and operator optimization | IN_PROGRESS | Nsys and NCU basic/detailed/NVLink/PM-sampling characterize the current source kernel. Active-grid truncation is retained without a speedup claim; a slower binary resolver was reverted. Large moved-volume, return-unshuffle, transfer-matrix, and compute-interference work remain. |
| C105 | Network-ready validation package | PLANNED | One-command build/warmup and off/force A/B runs, stable JSON counters/errors, canonical cases, and evidence labeling minimize real-cluster bring-up time. |
| C110 | Real-cluster Gin/RDMA scale and performance validation | DEFERRED_ENVIRONMENT | Requires multi-node rail/NIC environment; C080-H owns first correctness, while C110 owns repeated scale, performance, and fabric characterization. |
| C120 | Production fusion and auto policy | PLANNED | Fused implementation passes correctness and evidence-based performance gates. |

## Checkpoint update template

```text
ID / state / date:
Base commit and diff summary:
Commands/tests:
Results:
Failures retained:
Artifacts:
Decision:
Next checkpoint:
```
